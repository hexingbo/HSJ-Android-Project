<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.echronos.epoandroid">
    <!--package="com.echronos.epoandroid.module_weixin">-->

    <application>

        <!-- 在组件集成宿主时, 由于宿主已经声明了 CommonSDK 的公有 ConfigModule, 所以在这里只需要声明业务组件自己的 ConfigModule -->
        <meta-data
            android:name="com.echronos.epoandroid.app.GlobalConfiguration"
            android:value="ConfigModule" />
        <!-- 微信======开始 -->
        <activity
            android:name="com.echronos.epoandroid.wxapi.WXEntryActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:taskAffinity="net.sourceforge.simcpux"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <!-- 微信====结束 -->


    </application>
</manifest>
